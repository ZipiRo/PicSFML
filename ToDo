Add the posibility to add resources to the executable, like an icon, app version, etc.

Add the posibility if there is an icon set for the program to be set automaticaly

Add posibility to recreate, or update an existing .picsfml config file.

Add in the SFML framework a way to set the SFML window, like a settings struct.

Make it so that when you create a new template if there are already some files to skip them.


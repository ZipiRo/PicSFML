Add the posibility to add resources to the executable, like an icon, app version, etc.... DONE

Add the posibility if there is an icon set for the program to be set automaticaly. DONE

Add posibility to recreate, or update an existing .picsfml config file. DONE

Add a option to list all the values from the config file. DONE

Make it posible to update an existing config file for newer picsfml versions. 

Add in the SFML framework a way to set the SFML window, like a settings struct. DONE

Make it so that when you create a new template if there are already some files to stop the procedure. DONE

Remove cmd for Release version. DONE

Change the argument -g, --gcc to -c, --compiler becasue that makes more sense.

Despart the part wherer the args are read in other header files for better code structure

Check if the file paths exist.